services:
  - type: web
    name: movie-genre-api
    env: python
    buildCommand: pip install -r app/requirements.txt
    startCommand: uvicorn app.main:app --host=0.0.0.0 --port=$PORT
    workingDir: Movie genre Classification
